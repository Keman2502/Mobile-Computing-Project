<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.studentfacilitationsystem">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.StudentFacilitationSystem">
        <activity android:name=".portalnew" />
        <activity android:name=".quiz" />
        <activity android:name=".announcements" />
        <activity android:name=".delete_account" />
        <activity android:name=".drop_course" />
        <activity android:name=".update_account" />
        <activity android:name=".add_course" />
        <activity android:name=".portal" />
        <activity android:name=".signup" />
        <activity android:name=".login" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>